{
  "name": "ado-azure-tool",
  "private": true,
  "version": "99.99.99",
  "scripts": {
    "package": "npm run clean  && npm run compile  && tfx extension create --rev-version",
    "publish": "tfx extension publish --no-wait-validation --token $TOKEN",
    "publish-local": "tfx extension publish --no-wait-validation",
    "clean": "rimraf ./*.vsix",
    "compile": "tsc -p .",
    "ins-sql": "cd azure-sql && npm install",
    "up-sql": "cd azure-sql && npm run update",
    "test": "npm run compile && mocha tests/_suite.js",
    "update": "ncu -u && npm install"
  },
  "devDependencies": {
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.13",
    "azure-pipelines-task-lib": "^2.9.5",
    "minimist": ">=1.2.5",
    "mocha": "^8.0.1",
    "sync-request": "^6.1.0",
    "tfx-cli": "^0.8.1",
    "typescript": "3.9.5"
  },
  "dependencies": {}
}
